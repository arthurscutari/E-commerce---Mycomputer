

const swiper = new Swiper('.swiper', {
    // Optional parameters
    direction: 'horizontal',
    loop: true,
    autoplay:{
      delay:3000,
    },
    speed:1000,
    preventClicks: false,
    pagination: {
      el: '.swiper-pagination',
    },
  
  
  });